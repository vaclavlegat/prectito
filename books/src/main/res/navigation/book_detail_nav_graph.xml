<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/book_detail_nav_graph"
    app:startDestination="@id/bookDetailFragment">

    <fragment
        android:id="@+id/bookDetailFragment"
        android:name="cz.legat.books.ui.detail.BookDetailFragment"
        android:label="Detail"
        tools:layout="@layout/pt_book_detail_fragment">
        <argument
            android:name="id"
            app:argType="string" />
    </fragment>

    <fragment
        android:id="@+id/bookCommentsFragment"
        android:name="cz.legat.books.ui.detail.BookCommentsFragment"
        android:label="Detail"
        tools:layout="@layout/pt_book_detail_fragment">
        <argument
            android:name="id"
            app:argType="string" />
    </fragment>

    <fragment
        android:id="@+id/aboutFragment"
        android:name="cz.legat.books.ui.detail.BookDetailAboutFragment"
        android:label="About"
        tools:layout="@layout/pt_book_detail_about_fragment">
        <argument
            android:name="about"
            app:argType="string" />
    </fragment>


</navigation>